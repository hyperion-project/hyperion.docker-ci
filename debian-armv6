ARG SUITE=bullseye
ARG QT_VERSION=6
ARG REPOSITORY="https://github.com/hyperion-project"
ARG CMAKE_VERSION="3.28.3"

FROM --platform=$BUILDPLATFORM debian:bookworm-slim AS rootfs-builder

ARG SUITE
ARG BUILDPLATFORM

# sudo apt-get update && sudo apt-get install -y --no-install-recommends debuerreotype debootstrap gnupg qemu-user-static xz-utils

# WORKDIR="$(mktemp -d /tmp/raspbian-build.XXXXXXXX)"

# wget -O "${WORKDIR}/raspbian-archive-keyring.pgp.asc" "https://archive.raspbian.org/raspbian.public.key"
# gpg --batch --no-default-keyring --keyring "/usr/share/keyrings/raspbian-archive-keyring.pgp" --import "${WORKDIR}/raspbian-archive-keyring.pgp.asc"

# wget -O "${WORKDIR}/Release.gpg" "http://archive.raspbian.org/raspbian/dists/${{ inputs.codename }}/Release.gpg"
# wget -O "${WORKDIR}/Release" "http://archive.raspbian.org/raspbian/dists/${{ inputs.codename }}/Release"
# gpgv --keyring /usr/share/keyrings/raspbian-archive-keyring.pgp "${WORKDIR}/Release.gpg" "${WORKDIR}/Release"

# sudo debuerreotype-init --arch "armhf" --non-debian --keyring "/usr/share/keyrings/raspbian-archive-keyring.pgp" "${WORKDIR}/rootfs" "${{ inputs.codename }}" "http://raspbian.raspberrypi.org/raspbian/"
# sudo debuerreotype-minimizing-config "${WORKDIR}/rootfs"

# sudo debuerreotype-tar "${WORKDIR}/rootfs" "${WORKDIR}/rootfs.tar.xz"
# sudo chown "$(id -u):$(id -g)" "${WORKDIR}/rootfs.tar.xz"

# xzcat "${WORKDIR}/rootfs.tar.xz" | docker import --platform "linux/arm/v6" --change 'CMD ["/bin/bash"]' - raspbian:${{ inputs.codename }}
# docker image tag raspbian:${{ inputs.codename }} localhost:5000/raspbian:${{ inputs.codename }}
# docker image push localhost:5000/raspbian:${{ inputs.codename }}

# sudo rm -rf "${WORKDIR}"

RUN --security=insecure \
    apt-get update; \
    apt-get install -y --no-install-recommends debuerreotype debootstrap gnupg wget xz-utils qemu-user-static; \
    wget -O /tmp/raspbian.asc "https://archive.raspbian.org/raspbian.public.key"; \
    gpg --batch --no-default-keyring --keyring "/tmp/raspbian-archive-keyring.pgp" --import /tmp/raspbian.asc; \
    wget -O /tmp/Release.gpg "http://archive.raspbian.org/raspbian/dists/${SUITE}/Release.gpg"; \
    wget -O /tmp/Release     "http://archive.raspbian.org/raspbian/dists/${SUITE}/Release"; \
    gpgv --keyring /tmp/raspbian-archive-keyring.pgp /tmp/Release.gpg /tmp/Release; \
    cp /tmp/raspbian-archive-keyring.pgp /usr/share/keyrings/raspbian-archive-keyring.pgp; \
    debuerreotype-init --arch "armhf" --non-debian --keyring "/usr/share/keyrings/raspbian-archive-keyring.pgp" /tmp/rootfs "${SUITE}" "http://raspbian.raspberrypi.org/raspbian/"; \
    debuerreotype-minimizing-config /tmp/rootfs

FROM scratch AS raspbian-base
COPY --from=rootfs-builder /tmp/rootfs /
CMD ["/bin/bash"]

FROM raspbian-base

ARG SUITE
ARG QT_VERSION
ARG REPOSITORY
ARG CMAKE_VERSION
ARG DEBIAN_FRONTEND=noninteractive
ARG TARGETPLATFORM

ENV QEMU_EXECVE=1
ENV TARGETPLATFORM=${TARGETPLATFORM:-linux/arm/v6}

RUN apt-get update

RUN if [ "$SUITE" != "forky" ]; then \
    apt-get -y install --no-install-recommends ca-certificates curl gnupg; \
    curl -fsSL https://github.com/raspberrypi/rpi-image-gen/raw/master/keydir/raspberrypi-archive-keyring.gpg \
      | gpg --dearmor -o /usr/share/keyrings/raspberrypi-archive-keyring.gpg; \
    echo "deb [signed-by=/usr/share/keyrings/raspberrypi-archive-keyring.gpg] http://archive.raspberrypi.org/debian/ ${SUITE} main" \
      > /etc/apt/sources.list.d/raspi.list; \
    apt-get update; \
fi

RUN if [ "$SUITE" = "bookworm" ] || [ "$SUITE" = "trixie" ] || [ "$SUITE" = "forky" ] && [ "$QT_VERSION" = "6" ]; then \
    apt-get -y install qt6-base-dev libqt6sql6-sqlite qt6-serialport-dev qt6-websockets-dev \
      libxkbcommon-dev libvulkan-dev libgl1-mesa-dev; \
else \
    apt-get -y install qtbase5-dev libqt5serialport5-dev libqt5websockets5-dev \
      libqt5sql5-sqlite libqt5svg5-dev; \
fi

RUN apt-get -y install \
    devscripts fakeroot debhelper libdistro-info-perl git python3-dev \
    build-essential ninja-build libusb-1.0-0-dev libcec-dev libp8-platform-dev \
    libudev-dev libavahi-core-dev libavahi-compat-libdnssd-dev zlib1g-dev \
    libasound2-dev libjpeg-dev libturbojpeg0-dev libmbedtls-dev libftdi1-dev \
    libssl-dev libglib2.0-dev libdrm-dev

RUN if [ "$SUITE" != "trixie" ] && [ "$SUITE" != "forky" ]; then \
    apt-get -y install --no-install-recommends libraspberrypi-dev; \
fi

RUN git config --system --add safe.directory /source

ENV CMAKE_URL="https://github.com/Kitware/CMake/releases/download/v${CMAKE_VERSION}/cmake-${CMAKE_VERSION}"
RUN mkdir -p /tmp/cmake && cd /tmp/cmake && \
    curl -kL ${CMAKE_URL}.tar.gz | tar zxf - -C . --strip-components 1 && \
    sed -i -e 's/|aarch64//g' Source/Checks/cm_cxx_features.cmake && \
    ./bootstrap --prefix=/usr --parallel=$(nproc) && \
    make -j$(nproc) && make install && \
    cd && rm -rf /tmp/cmake

RUN cmake --version

RUN curl -kL https://github.com/balena-io/qemu/releases/download/v7.0.0%2Bbalena1/qemu-7.0.0.balena1-arm.tar.gz \
    | tar zxvf - -C . --strip-components 1 && mv qemu-arm-static /usr/bin/qemu-static
RUN chmod +x /usr/bin/qemu-static

ENTRYPOINT ["/usr/bin/qemu-static"]